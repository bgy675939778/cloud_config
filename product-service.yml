server:
  port: 8771


eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
#服务名称
spring:
  application:
    name: product-service
  zipkin:
    base-url: http://localhost:9411
  #配置采样百分比
  sleuth:
    sampler:
      probability: 1